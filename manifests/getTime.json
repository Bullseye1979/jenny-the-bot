{
  "name": "getTime",
  "description": "Return the current date and time in UTC (ISO 8601). Always call this tool when the current time or date is needed — never use timestamps or dates from conversation history.",
  "policyHint": "When asked about the current date, time, or day of the week, call this tool immediately — never guess or use timestamps from conversation history.",
  "parameters": {
    "type": "object",
    "properties": {},
    "additionalProperties": false
  }
}
