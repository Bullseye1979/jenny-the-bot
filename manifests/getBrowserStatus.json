{
  "name": "getBrowserStatus",
  "description": "Retrieves the current browser status (active tab URL and title) from the user's Jenny browser extension. Use browserCode only when the user mentioned a Jenny browser code in the chat and normal web-login identity is not available or does not return a status.",
  "parameters": {
    "type": "object",
    "properties": {
      "browserCode": {
        "type": "string",
        "description": "Optional Jenny browser code shown by the extension. Use only as a fallback when web-login user identity is unavailable."
      }
    },
    "additionalProperties": false
  }
}
